

const object = {
    0: 'mango',
    1: 'apple',
    2: 'lichi'
}

console.log(object[0]);


// array of numbers
const arr = [28, 48, 90]
console.log(arr);

// array of strings
const fruits = ['fsdk', 'fs ', 'fsdfs', 'fsdkfs']
console.log(fruits);

// array of boolean values
const bool = [true, false]
console.log(bool);

// array of multiple data types
const mix = [true, 38, 'hamza', undefined, null]
console.log(mix);


// accessing array elements
const veggies = ['carrot', 'tomato']
console.log(veggies[1]);

// Modifying array element by index number
veggies[0] = 'lady finger'
console.log(veggies);


